"""Helper utilities."""


def format_name(first: str, last: str) -> str:
    """Format a full name.

    See [User][my_pkg.models.User] for the model that uses this.
    """
    return f"{first} {last}"
